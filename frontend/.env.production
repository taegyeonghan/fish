# 운영 빌드(npm run build) 기본값 — 이 파일 없이 빌드하면 API base 가 개발용
# http://localhost:5001 로 굳어 실사용자 환경에서 전 라우트가 연결 거부된다.
#
# 백엔드는 SPA 를 /world-model/ 로, forecast 라우터를 /world-model-api/* 로 서빙한다.
# 루트 상대 경로라 호스트·포트·터널 주소에 묶이지 않는다.
VITE_API_BASE_URL=/world-model-api
VITE_BASE=/world-model/
